[project]
name = "blog"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "jake", email = "jacob.harris.122@gmail.com"},
]
dependencies = ["mkdocs>=1.6.1", "mkdocs-material>=9.7.6"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
